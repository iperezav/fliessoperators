
class meta{
  //inputs: m;
  //size of words=J;
  public:

  void getmeta(const int m, const int J)
  nrows=(pow(m+1,J+1)-1)/m-1;
  ncols=J;

  int allprod[nrows][ncols+1];
  for (int i=1;i<J+1;i++){
    sizeword=i;
    accwrdssizeim1=(m+1)*(pow(m+1,sizeword-1)/m);
    accwrdssizei=(m+1)*(pow(m+1,sizeword)/m);
    startrowi=accwrdssizeim1;
    endrowi1=accwrdssizei;
    
    for (int j=startrowi;j<endrowi1;j++){
      for (int ccol=J;ccol>=J;ccol--){
        arrmeta[j][ccol]=;
        
      }
        
        
        
      }
    
  for each i in array 0:m  // 
    arrayfinal=
  }
  

  
}
